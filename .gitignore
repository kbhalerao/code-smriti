# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib64/
lib/ingestion-worker/venv/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
venv/
.venv/
ENV/
env/

# Environment variables
.env
.env.local

# MCP configuration (contains credentials)
.mcp.json

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
.claude/settings.local.json

# Logs
*.log
logs/

# Docker
*.pid
*.seed
*.pid.lock

# Database
couchbase_data/

# Repositories (cloned during ingestion)
repos/

# Model cache
.cache/
model_cache/

# SSL certificates and credentials
api-gateway/ssl/
api-gateway/certbot/
*.pem
*.key
*.crt

# Archive directories (historical development artifacts)
.archive/

# Repo ingestion config (contains private org/repo names)
config/repos_to_ingest.txt

# Eval data (machine-generated, contains private repo names)
evals/results/
evals/v4_eval_questions.json
services/api-server/tests/search_eval_questions.json

# Ingestion config (contains private repo names)
services/ingestion-worker/ingestion_config.yaml

# Ad-hoc dependency analysis dumps (machine-generated, contain private repo names)
/t1deps.json
/t2deps.json
/t3deps.json
/t1deps_criticality.json
/farmworthdb_all_deps.json
/farmworthdb_criticality.json

# Generated BDR briefs (LLM output from test/eval runs)
services/ingestion-worker/scripts/bdr_brief_*.md

# Generated KPI dashboard (regenerated daily by LaunchAgent)
landing/kpi.html

# Temporary files
*.tmp
*.temp
.pytest_cache/
.coverage
htmlcov/

# Mac
.DS_Store
.AppleDouble
.LSOverride

# Node (if any frontend is added later)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Off-LAN /llm allowlist entries. These name third-party production
# infrastructure and this repo is public; only the .example is tracked.
services/api-gateway/llm-allowlist.d/*.conf
!services/api-gateway/llm-allowlist.d/*.conf.example
