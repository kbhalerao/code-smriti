[project]
name = "code-fetcher"
version = "0.1.0"
description = "Shared library for retrieving code from repositories"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
